# cerase-docreader MCP — Python dependency lock (OPT-15 / TOOLS-2).
# markitdown[all] pulls the per-format converters (pdf/docx/xlsx/pptx/
# html/epub/...). No LLM client — docreader makes no upstream call.
#
# mcp stays on 1.x, and the lock holds the mcp and mcp-proxy the universal
# runner pins. mcp-proxy is this image's entrypoint, asks only for mcp>=1.17, and
# imports `request_ctx` from `mcp.server.lowlevel.server`, which the 2.x SDK
# removed: under 2.x the image builds and the container exits on its first
# import. server.py uses the 1.x API (`FastMCP`) until an mcp-proxy release
# imports under 2.x.
mcp>=1.30.0,<2.0
mcp-proxy>=0.12.0,<1.0
markitdown[all]>=0.0.1,<1.0
